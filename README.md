A simple cli tool to help you setup an anchor multi-folder starter project with a counter program, tests using `bun:test` written using `@solana/kit`, codama codegen setup.

To add:
- [ ] Add a frontend
- [ ] Write tests